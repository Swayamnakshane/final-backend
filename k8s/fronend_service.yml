kind: Service
apiVersion: v1
metadata:
  name: front-svc
  namespace: exam
  labels:
    app: app
spec:
  type: NodePort
  selector:
    app: app
  ports:
    - protocol: TCP
      targetPort: 3000
      port: 3000
      nodePort: 31000
