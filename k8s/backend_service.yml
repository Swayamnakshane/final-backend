kind: Service
apiVersion: v1
metadata:
  name: backend-svc
  namespace: exam
  labels:
    app: backapp
spec:
  type: NodePort
  selector:
    app: backapp
  ports:
    - protocol: TCP
      targetPort: 5000
      port: 5000
      nodePort: 30080
